import React from "react";
import Container from '@material-ui/core/Container';

export const Submit = () => {
    return ( <
        Container maxWidth = "sm"
        style = {
            { marginTop: '4rem' }
        } >
        <
        h3 > Thank you
        for finalizing the registration! < /h3>

        <
        /Container>
    );
};